apiVersion: v1
kind: Service
metadata:
  name: api-gateway
  namespace: cake-delight
spec:
  type: NodePort # Changed from ClusterIP
  selector:
    app: api-gateway
  ports:
    - name: http
      port: 5000
      targetPort: 5000
      nodePort: 30050 # Kind will map this port